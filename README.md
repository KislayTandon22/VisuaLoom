# VisuaLoom
A local, privacy-preserving GenAI system that indexes all your images, detects faces and objects, labels them automatically, and lets you search using natural language — all powered by open-source vision models + a local LLM (Ollama)
